#ifndef SHELL_H
#define SHELL_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/wait.h>
#include <unistd.h>
#include <sys/types.h>

/* Core Functions */

/* Helper Functions */

/* Find a the length of a string */
int _strlen(char *s);

/* Determine whether two strings are identical */
int _strcmp(char *s1, char *s2);


/* SHELL_H end */
#endif
